# OPTIONAL extra sources (untagged).
#
# The curated, protocol-TAGGED source list now lives in sources.json — that is
# the primary source of truth and is preferred because tagging lets the
# validator try only ONE protocol per proxy (much faster).
#
# Any raw-list URL you add here (one per line) is also fetched, but UNTAGGED,
# so each of its proxies will be tried against all of http/socks4/socks5.
# Use this only for quick experiments; for permanent sources add them to
# sources.json with a "protocol" instead.
